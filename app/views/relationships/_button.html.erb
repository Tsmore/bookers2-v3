<% if current_user != user %>
  <% if current_user.following?(user) %>
    <%= link_to "Following", user_relationships_path(user.id), remote: true, method: :delete %>
  <% else %>
    <%= link_to "Follow", user_relationships_path(user.id), remote: true, method: :post %>
  <% end %>
<% end %>